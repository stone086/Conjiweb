# Conjiweb 2.1.0 Validation Report

Release: `2.1.0 compatibility-chaos`

## Scope

This release adds browser compatibility diagnostics, Safari/iOS known-issue documentation, and chaos recovery evidence templates for network, token, Prosody, MinIO, and in-flight browser-close failures.

## Static validation completed

| Check | Result |
| --- | --- |
| `bash scripts/compatibility_chaos_validate.sh` | Passed |
| `bash scripts/stage0_validate.sh --local` | Passed, 0 warnings |
| `bash scripts/trusted_build_validate.sh --local` | Passed |
| `bash scripts/observability_validate.sh` | Passed |
| `bash scripts/performance_validate.sh` | Passed; k6 inspect skipped locally because k6 is not installed |
| `bash scripts/omemo_runtime_validate.sh` | Passed |
| `bash -n install.sh manage.sh uninstall.sh` | Passed |
| `python3 -m compileall -q apps/api/app apps/api/alembic` | Passed |
| `npm ci` | Passed |
| `npm run build` | Passed |
| `npm test -- --run --reporter=dot` | Passed: 5 test files / 20 tests |
| `npm audit --omit=dev --audit-level=high` | Passed: 0 vulnerabilities |

## Known non-blocking warnings

- Vite still reports large chunks after minification; this remains a performance/code-splitting follow-up.
- `@privacyresearch/curve25519-typescript` still imports Node `path`/`fs` from its browser bundle path; Vite externalizes them. Build succeeds, but OMEMO browser runtime tests should keep watching this.
- `npm ci` reports moderate dev/transitive advisories outside the production high-severity audit gate.

## Staging evidence still required

- Real Chrome/Edge, Firefox, Safari macOS/iOS, and Samsung Internet matrix.
- DevTools offline send/reconnect evidence.
- Expired-token upload recovery evidence.
- Prosody restart recovery evidence.
- MinIO outage upload error/retry evidence.
- Browser-close in-flight send reconciliation evidence.

Use `docs/compatibility/compatibility-chaos-results.md` for the staging result log.
