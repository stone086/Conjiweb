# Conjiweb 2.3.4 validation report

Version: `2.3.4-prestaging-i18n-closure`

## Purpose

This release closes items that can be handled before real staging:

- organize the post-2.3.3 ToDoList into actionable gates;
- make real-staging blockers explicit;
- complete the English/Simplified Chinese language-pack gate;
- add i18n tests and validation scripts.

## Validation performed

- `bash scripts/i18n_validate.sh`
- `bash -n install.sh`
- `bash -n manage.sh`
- `bash -n uninstall.sh`
- `python3 -m compileall -q apps/api/app apps/api/alembic`
- `bash scripts/stage0_validate.sh --local`
- `bash scripts/preflight_hardening_validate.sh`

## 2026-05-10 live validation addendum

Target: `https://example.com`
Commit: `e80f439`

Additional checks run after deployment:

- `bash scripts/preflight_check.sh --strict`
  - Passed on the live VPS.
  - Expected warnings: ports 80, 443, 5222, and 3478 are already listening
    because Conjiweb services are running.
- `sudo bash scripts/stage0_validate.sh --server --domain example.com`
  - Passed with 0 warnings.
  - Verified nginx config, Prosody config, systemd services, local/public
    health checks, and Alembic head `0009_attachment_owner_account`.
- `bash scripts/performance_validate.sh`
  - Passed static performance validation.
- `bash scripts/compatibility_chaos_validate.sh`
  - Passed static compatibility/chaos validation.
- `bash scripts/observability_validate.sh`
  - Passed static observability validation.
- `bash scripts/evidence_gate_validate.sh --local`
  - Passed evidence file gate.
- `cmd /c npm run build` in `apps/web`
  - Passed TypeScript and Vite production build.
  - Vite emitted existing chunk-size and mixed static/dynamic import warnings.
- `cmd /c npm run test` in `apps/web`
  - Passed: 6 test files, 23 tests.
- API Python syntax check on VPS
  - Passed with `find apps/api -name '*.py' ... | xargs python3 -m py_compile`.
- `cmd /c npm audit --omit=dev` in `apps/web`
  - Passed with 0 vulnerabilities.
- `bash scripts/preflight_hardening_validate.sh`
  - Passed on the live VPS.
- `bash scripts/external_audit_validate.sh`
  - Passed on the live VPS.
- `bash scripts/staging_evidence_collect.sh --domain example.com`
  - Passed and wrote `docs/evidence/staging-runs/20260510T141405Z` on the VPS.
  - `/api/health` evidence returned `{"status":"ok","version":"2.3.4"}`.
  - `/api/metrics` evidence returned 403, matching restricted metrics access.
- Live log scan
  - No `request_failed`, `csp_violation`, `Traceback`, `ERROR`, or `CRITICAL`
    entries in the recent `conjiweb-api` journal window.

Authenticated live attachment regression:

- `cjw001` uploaded a PNG named `真机复测-中文图片.png`.
- Owner download returned `200 image/png`.
- Owner download after simulated 10-minute attachment age still returned
  `200 image/png`.
- `cjw002` cross-account download returned `404`.
- `Content-Disposition` included `filename*=UTF-8`.

Additional live API smoke:

- `/api/auth/config` returned 200 with domain config.
- `/api/auth/admin/login` with a wrong password returned 401.
- `/api/auth/user-token` with a wrong password returned 401.
- `/files/...` without authentication returned 401.
- `/api/metrics` from the public internet returned 403.

Known remaining validation gaps:

- Local API pytest was not run because the Windows launcher exists but no
  default Python runtime is installed.
- API `pytest` was not run on the production VPS because the runtime venv does
  not include `pytest`; run it in a dev/test environment or CI.
- `pg_stat_statements` is not enabled on the live PostgreSQL instance, so
  hot-query evidence is still pending.
- Prometheus/Alertmanager live target and alert-fire evidence is still pending.
- Real browser/device matrix is still pending for Safari, iOS PWA, Android, and
  push/offline/call behavior.
- Destructive restore was intentionally not run on the live target; complete it
  only on disposable staging data.

## Real-staging pending

See `docs/roadmap/POST_2.3.3_EXECUTION_PLAN.md`.
