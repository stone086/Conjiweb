# Conjiweb 2.3.4 validation report

Version: `2.3.4-prestaging-i18n-closure`

## Purpose

This release closes items that can be handled before real staging:

- organize the post-2.3.3 ToDoList into actionable gates;
- make real-staging blockers explicit;
- complete the English/Simplified Chinese language-pack gate;
- add i18n tests and validation scripts.

## Validation performed

- `bash scripts/i18n_validate.sh`
- `bash -n install.sh`
- `bash -n manage.sh`
- `bash -n uninstall.sh`
- `python3 -m compileall -q apps/api/app apps/api/alembic`
- `bash scripts/stage0_validate.sh --local`
- `bash scripts/preflight_hardening_validate.sh`

Frontend `npm ci && npm run build` should still be re-run in a normal npm network environment before staging because the current sandbox may not have reliable registry access.

## Real-staging pending

See `docs/roadmap/POST_2.3.3_EXECUTION_PLAN.md`.
