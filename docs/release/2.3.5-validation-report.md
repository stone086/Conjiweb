# Conjiweb 2.3.5 validation report

Version: `2.3.5-staging-complete`

## Purpose

This release records the completed staging validation pass and fixes the
staging install blocker found during the fresh-install test.

## Changes

- Bumped version metadata from `2.3.4` to `2.3.5`.
- Regenerated `apps/api/requirements.txt` so `pip install --require-hashes`
  works on Ubuntu 22.04 / CPython 3.10 staging.
- Added fresh-install evidence to `docs/evidence/install/staging-install-results.md`.
- Added the fresh-install evidence file to `scripts/evidence_gate_validate.sh`.
- Updated staging evidence for monitoring, performance, and backup/restore.

## Staging Validation

Target: `https://staging.example.com`

Completed the full staging validation flow:

1. Fresh install with `bash install.sh --run-local --ssh-port 22`: PASS
2. `bash scripts/preflight_check.sh --strict`: PASS
3. `bash scripts/stage0_validate.sh --server --domain staging.example.com`: PASS
4. Browser smoke: PASS
5. k6 low-concurrency baseline: PASS
6. DB performance collection: COMPLETE
7. Prometheus/Alertmanager real-target alert smoke: PASS
8. Destructive backup/restore drill: PASS
9. `bash scripts/evidence_gate_validate.sh`: PASS
10. Evidence files updated in the repository: COMPLETE

## Post-Install Health

```text
postgresql active
redis-server active
minio active
prosody active
coturn active
conjiweb-api active
nginx active
prometheus active
prometheus-alertmanager active
```

```text
curl https://staging.example.com/api/health
{"status":"ok","version":"2.3.5"}
```

## Notes

- The first fresh-install attempt exposed a real issue: the previous Python
  hash lock was generated for CPython 3.13 wheels, while staging uses Python
  3.10. The repaired lock now includes the Python 3.10 wheel hashes needed by
  staging.
- Non-interactive SSH install left firewall prompts at their default `no`, so
  UFW was not reset during remote validation.
