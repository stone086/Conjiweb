# Conjiweb 1.6.4 Validation Report

Base: conjiweb-1.6.3-stage0-validation
Release: conjiweb-1.6.4-frontend-closure

## Fixed

- LoginPage: REST `/api/auth/user-token` 401 fallback no longer blocks a successful XMPP login.
- DiscoveryPage: replaced stale `addOrUpdateConversation` usage with `upsertConversation`, and replaced `lastMessageBody` with `lastMessage`.
- RightPanel: replaced stale `contact.nickname` with `contact.name` fallback.
- RightPanel: removed duplicate closing `</ul>` tag.
- MessageView / GroupPanel / aesgcmMedia / i18n: fixed additional TypeScript build blockers discovered during the real build.
- Added `apps/web/package-lock.json`.
- Upgraded axios to `1.16.0` and added npm overrides for patched transitive `@xmldom/xmldom` and `dompurify`.

## Verification Run

- `npm ci`: passed
- `npm run build`: passed
- `npm audit --omit=dev --audit-level=high`: passed, 0 vulnerabilities
- `bash scripts/stage0_validate.sh --local`: passed, 0 warnings
- `bash -n install.sh`: passed
- `bash -n manage.sh`: passed
- `bash -n uninstall.sh`: passed
- `python3 -m compileall -q apps/api/app apps/api/alembic`: passed

## Notes

Vite still emits non-fatal warnings about large chunks and mixed static/dynamic imports. These do not fail the production build and should be addressed later in the performance/code-splitting stage.
