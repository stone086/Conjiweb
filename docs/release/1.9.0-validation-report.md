# Conjiweb 1.9.0 Validation Report

Release: `1.9.0-observability`

## Local/static validation

- `bash -n install.sh manage.sh uninstall.sh` — pass
- `python3 -m compileall -q apps/api/app apps/api/alembic` — pass
- `bash scripts/stage0_validate.sh --local` — pass
- `bash scripts/trusted_build_validate.sh --local` — pass
- `bash scripts/omemo_runtime_validate.sh` — pass
- `bash scripts/observability_validate.sh` — pass

## Frontend validation

- `npm ci` — pass
- `npm run build` — pass
- `npm test` — pass
- `npm audit --omit=dev --audit-level=high` — pass in release environment

## Observability checks

- JSON log formatter compiles and is wired into `main.py`.
- Request middleware records request count and latency histogram metrics.
- `/metrics` route renders Prometheus text format.
- Nginx exposes `/api/metrics` with localhost/private-network allow rules.
- Alert rules are present at `configs/prometheus/conjiweb-alerts.yml`.

## Staging tasks still required

- Scrape `/api/metrics` from a real Prometheus instance.
- Import alert rules into Alertmanager and trigger test alerts.
- Confirm log collector parses JSON fields from systemd journal.
- Confirm nginx allow list matches the actual Prometheus network.
