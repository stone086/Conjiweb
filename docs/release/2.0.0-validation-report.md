# Conjiweb 2.0.0 Validation Report

Release: `2.0.0-performance-baseline`

## Scope

This release adds performance baselining tools rather than changing the chat
feature surface:

- k6 load-test examples for public API endpoints.
- SQLAlchemy query counting for N+1 detection.
- slow SQL structured log events.
- PostgreSQL index/hot-query review scripts.
- performance baseline documents and release gate script.

## Local validation

| Check | Result |
|---|---|
| `bash scripts/performance_validate.sh` | PASS |
| `bash scripts/stage0_validate.sh --local` | PASS |
| `bash scripts/trusted_build_validate.sh --local` | PASS |
| `bash scripts/observability_validate.sh` | PASS |
| `bash scripts/omemo_runtime_validate.sh` | PASS |
| `bash -n install.sh manage.sh uninstall.sh` | PASS |
| `python3 -m compileall -q apps/api/app apps/api/alembic` | PASS |
| `npm ci` | PASS |
| `npm run build` | PASS |
| `npm test -- --run` | PASS |
| `npm audit --omit=dev --audit-level=high` | PASS |

## Runtime evidence still required

These must be filled from staging before production promotion:

- k6 p50/p95/failure-rate results.
- CPU and memory peak under 25 and 100 VU scenarios.
- PostgreSQL `hot_queries.sql` output.
- PostgreSQL `index_usage.sql` review.
- confirmation that no core chat route repeatedly logs `n_plus_one_suspected`.
