# Conjiweb 1.7.0 Validation Report

## Scope

Stage 1 first pass: OMEMO trust model, fingerprint UI, and audit documentation.

## Local validation

- `npm ci`: pass
- `npm run build`: pass
- `npm test`: pass
- `npm audit --omit=dev --audit-level=high`: pass, 0 vulnerabilities
- `bash scripts/stage0_validate.sh --local`: pass
- `bash -n install.sh`: pass
- `bash -n manage.sh`: pass
- `bash -n uninstall.sh`: pass
- `python3 -m compileall -q apps/api/app apps/api/alembic`: pass

## Known non-blocking warnings

- Vite reports chunks larger than 500 kB after minification. This is not new and should be handled in the performance/code-splitting stage.

## Not yet claimed

- No real VPS staging install was run in this environment.
- No third-party OMEMO interoperability test was run in this environment.
- No cryptographic audit is claimed. `docs/security/OMEMO_AUDIT.md` is an implementation audit and test plan, not a formal proof.
