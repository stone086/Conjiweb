# Conjiweb 1.8.0 Validation Report

Package: `conjiweb-1.8.1-hash-pinned-python.zip`
Stage: 2 — trusted build

## Changes

- Bumped project version from 1.7.1 to 1.8.0.
- Added `apps/api/requirements.in` as the human-edited Python dependency input file.
- Kept `apps/web/package-lock.json` and wired validation to require it.
- Replaced the CI workflow with a trusted-build workflow for shell, web, API, SBOM, audit, Bandit, and CodeQL checks.
- Added `scripts/trusted_build_validate.sh`.
- Added `scripts/generate_sbom.sh`.
- Added `docs/security/TRUSTED_BUILD.md`.

## Local validation performed

- `bash -n install.sh`
- `bash -n manage.sh`
- `bash -n uninstall.sh`
- `bash -n scripts/stage0_validate.sh`
- `bash -n scripts/omemo_runtime_validate.sh`
- `bash -n scripts/trusted_build_validate.sh`
- `bash -n scripts/generate_sbom.sh`
- `bash scripts/trusted_build_validate.sh --local`
- `python3 -m compileall -q apps/api/app apps/api/alembic`

## Build validation

Frontend validation was run successfully:

```bash
cd apps/web
npm ci
npm run build
npm test
npm audit --omit=dev --audit-level=high
```

Results:

- `npm ci`: passed.
- `npm run build`: passed. Vite reported non-fatal large-chunk/code-splitting warnings.
- `npm test`: passed, 4 test files / 15 tests.
- `npm audit --omit=dev --audit-level=high`: passed, 0 production high vulnerabilities.

Note: full `npm ci` reported moderate vulnerabilities in the full dependency tree. The release gate currently blocks high production vulnerabilities; moderate findings should be reviewed during the next maintenance pass.

## Known limitation

Python `--require-hashes` is not enabled in `install.sh` yet because package hashes must be generated against exact resolved wheel/sdist artifacts by `pip-compile --generate-hashes` on an online trusted builder. This release adds the policy, input file, validation gate, and CI workflow; the next sub-release can flip install-time `--require-hashes` after hashes are generated and verified.
